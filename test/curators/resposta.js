const should = require('chai').should();
const expect = require('chai').expect;
const client = require('../waTestClient');

describe('Curators-resposta', function () {

    it('Deve aceitar', function (done) {
        done()
    });

});